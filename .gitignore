# Dependencies
node_modules

# Build output
dist
*.tsbuildinfo

# Secrets / local config
.env
.env.local

# Local layout state (personal dashboard arrangement)
layouts.json

# Opt-in shared connection (contains a token — never commit)
connection.json

# Personal Home Assistant config dumps
scripts/lovelace-dump.json
scripts/views.json

# Screenshots contain personal data; a curated subset is force-added
screenshots

# Recorded clips: raw webm scratch is throwaway; the curated mp4/jpg are force-added
media/.raw
media

# Personal layout backup for redeploying to Docker/add-on
my-dashboard-layout.backup.json

# Local dev-server launch config for Claude preview
.claude/
