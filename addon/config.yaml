name: Glance — HA Dashboard
version: "1.7.1"
slug: dynamic_ha_dashboard
description: A premium, fully-configurable Home Assistant dashboard with at-a-glance buttons, live tiles, and ambient effects.
url: https://github.com/jvenuto80/Dynamic-HA-Dashboard
arch:
  - aarch64
  - amd64
  - armv7
init: false
ingress: true
ingress_port: 3000
panel_icon: mdi:view-dashboard-variant
panel_title: Glance
ports:
  3000/tcp: null
ports_description:
  3000/tcp: Web UI port — optional. Leave empty to use Ingress only; set a host port (e.g. 3000) for direct/kiosk access (Fully Kiosk Browser, tablets).
options: {}
schema: {}
